ppi_maxSubG.csv is the PPI network extracted from IHP-PPI network by applying 0.7 cutoff, i.e., interactions with a confidence score of ≥ 0.7 were selected. All the proteins are connected in ppi_maxSubG.csv.

uni_protein.csv contains the names of the proteins involved in the ppi_maxSubG.csv.

instances_list.csv gives an example of synergy instance in the format of cell_drug1_drug2. 

full_sized_graph contains node table of the full sized graph created for cell line 22RV1. It was divided into three parts because of the file size limit of github.
